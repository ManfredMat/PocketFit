import styled from "styled-components";

const Styles = {
    Container: styled.div`
        padding: 3rem;
        display: flex;
        flex-direction: column;
        width:100vw;
        height: 100vh;
    `
}

export default Styles;